everytime you make changes to the server codebase and you want to run it, make sure you do `npx tsc --build` to transpile typescript back to javascript. then you can run it using `node dist/index.js`
